# PR assets

Orphan branch holding the screenshots referenced from pull-request descriptions.
Exactly one directory per pull request, named after its number (`pr-702/`,
`pr-703/`, ...) - no other layout. Never merged into main. See the "Screenshots
are mandatory for any UI change" section of CLAUDE.md for how to add to it.
