{
  "name": "workspace-alpine",
  "version": "1.0.0",
  "description": "esbuild bundle of Alpine.js and its plugins for the workspace Django project. Alpine evaluates every component expression and holds decrypted vault entries in its reactive state, so it is served from our own statics rather than a CDN. Rebuild with `npm run build:alpine` after bumping the pinned versions.",
  "private": true,
  "scripts": {
    "build:alpine": "esbuild alpine-bundle.js --bundle --format=iife --minify --target=es2022 --outfile=../../workspace/common/static/ui/js/vendor/alpine/alpine.js"
  },
  "dependencies": {
    "alpinejs": "3.16.0",
    "@alpinejs/collapse": "3.16.0",
    "@imacrayon/alpine-ajax": "0.12.7"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
